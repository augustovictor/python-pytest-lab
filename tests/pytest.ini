[pytest]
norecursedirs = .* venv models utils
testpaths = tests
minversion = 3.0
addopts = -rsxX -l --tb=short --strict
markers =
    smoke: Run the smoke test functions
    get: Run the test functions regarding GET methods